// COMPONENTS
const switchControls = document.getElementsByClassName('switch');
const formControls = document.getElementsByClassName('form-control');
const form = document.getElementById('form');
const formControlName = document.getElementById('formControlName');
const formControlPhone = document.getElementById('formControlPhone');
const body = document.getElementById('body');
const container = document.getElementById('container');

// PARAMETRS
const classSwitchActive = 'switch-active';